services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/model:ro
    env_file:
      - .env
    environment:
      - MODEL_PATH=model/extended_20ep/weights/best.pt
    restart: unless-stopped
